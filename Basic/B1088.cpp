#include<iostream>
#include<cmath>
using namespace std;

int main (void)
{
    //模拟方程
    int jia, yi, M, X, Y;
    double bing;

    cin >> M >> X >> Y;
    for (int i = 99;i > 9;i-- ){
        yi = (jia % 10 * 10 + x % 10);
        bing = abs(jia - yi) / (double)(X);
        if (yi == Y * bing)
    }




    return 0;
}
